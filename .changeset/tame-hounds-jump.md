---
"spheres": minor
---

Introduce `ElementIdentifier` for referencing DOM elements from a command manager. Custom command managers can now resolve elements via `withDomActions`/`DomCommandManager`/`GetElement`.
