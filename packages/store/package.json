{
  "name": "@spheres/store",
  "version": "1.0.0",
  "description": "Easy state management",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist/"
  ],
  "scripts": {
    "prepublishOnly": "npm run build",
    "build": "tsc --build",
    "clean": "rm -rf ./dist && rm -rf ./tsconfig.tsbuildinfo",
    "test": "best --behaviors './behaviors/**/*.behavior.ts' --runInBrowser '**/*' --failFast --viteConfigPath './behaviors/vite.config.js'"
  },
  "author": "Brian Watkins",
  "license": "MIT",
  "devDependencies": {
    "best-behavior": "^0.1.1",
    "esbehavior": "^4.2.2",
    "great-expectations": "^1.5.1",
    "playwright": "^1.40.0",
    "typescript": "^5.0.2",
    "vite": "^5.0.0",
    "vite-tsconfig-paths": "^4.2.0"
  }
}
